package vcsc.teamcode.opmodes.test;

public class BeautifulAuto {
    public void init() {
        /*ActionBuilder scorePreload = new ActionBuilder(specimenPose)
                .followPath(scorePreload)
                .then(scoreSpecimen);

        ActionBuilder sampleMovingPosition = new ActionBuilder()
                .followPath(sampleMovingPosition)
                .then(intakePoseAuto);

        ActionBuilder moveSample1 = new ActionBuilder()
                .followPath(sample1)
                .then(sampleMovingPosition);*/
    }
}
