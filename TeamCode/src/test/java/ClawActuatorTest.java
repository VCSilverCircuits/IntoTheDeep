/*import org.jetbrains.annotations.TestOnly;
import org.junit.Test;

import vcsc.teamcode.component.claw.ClawActuator;
import vcsc.core.mock.MockServo;
import vcsc.teamcode.component.claw.ClawState;

public class ClawActuatorTest {
    @Test
    public void testUpdateState() {
        // Arrange
        MockServo mockServo = new MockServo(System.currentTimeMillis());
        mockServo.forcePosition(0.8);
        ClawState clawState = new ClawState();
        clawState.setPosition(0);
        ClawActuator clawActuator = new ClawActuator(mockServo);

        // Act
        clawActuator.updateState(clawState);

        // Assert
        assert (clawState.getPosition() == 0);
    }
}*/
